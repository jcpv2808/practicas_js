const 
    suma = (a,b) => a+b,
    resta = (a,b) => a-b,
    multiplicacion = (a,b) => a*b,
    division = (a,b) => a/b,
    resto = (a,b) => a%b;

export const calculo = {
    suma,
    resta,
    multiplicacion,
    division,
    resto,
}